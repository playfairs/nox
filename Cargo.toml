[package]
name = "nox"
version = "0.1.1"
edition = "2024"
description = "The Nox Build System"
license = "Unlicense"

[profile.release]
lto = true
codegen-units = 1
strip = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
